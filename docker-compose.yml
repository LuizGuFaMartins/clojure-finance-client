version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finance-client
    env_file:
      - .env
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"

